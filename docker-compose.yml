services:
  argus-dashboard:
    build: .
    image: argus-dashboard:latest
    container_name: argus-dashboard
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    restart: unless-stopped
